apiVersion: v1
kind: Service
metadata:
  name: java-error-handling-otel
  namespace: java-error-handling-lab
spec:
  selector:
    app: java-error-handling-otel
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
